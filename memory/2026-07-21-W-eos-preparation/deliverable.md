# Deliverable

W EOS generation is complete: 57 structures (bcc/fcc/hcp, 19 scales each)
and their metadata are isolated under `results/W_eos_benchmark/eos_reference/`.
Protocol-B DFT submission is approved with the VASP backend defaults and the
three phase jobs 12448--12450 completed successfully. The validated,
fixed W EOS reference is
`results/W_eos_benchmark/eos_reference/eos_reference.csv`; it remains
validation-only and must never enter a training DB.
