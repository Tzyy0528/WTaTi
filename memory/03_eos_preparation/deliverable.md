# Deliverable: EOS Preparation: W, Ta, and Ti

This record groups the common workflow stage only. W, Ta, and Ti data, calculations, and outputs remain strictly independent.

## W

W EOS generation is complete: 57 structures (bcc/fcc/hcp, 19 scales each)
and their metadata are isolated under `results/W_eos_benchmark/eos_reference/`.
Protocol-B DFT submission is approved with the VASP backend defaults and the
three phase jobs 12448--12450 completed successfully. The validated,
fixed W EOS reference is
`results/W_eos_benchmark/eos_reference/eos_reference.csv`; it remains
validation-only and must never enter a training DB.

## Ta

Ta EOS generation is complete: 57 structures (bcc/fcc/hcp, 19 scales each)
and their metadata are isolated under `results/Ta_eos_benchmark/eos_reference/`.
Protocol-B DFT submission is approved with the VASP backend defaults and the
three phase jobs 12451--12453 completed successfully. The validated,
fixed Ta EOS reference is
`results/Ta_eos_benchmark/eos_reference/eos_reference.csv`; it remains
validation-only and must never enter a training DB.

## Ti

Ti EOS generation is complete: 57 structures (bcc/fcc/hcp, 19 scales each)
and their metadata are isolated under `results/Ti_eos_benchmark/eos_reference/`.
Protocol-B DFT submission is approved with the VASP backend defaults and the
three phase jobs 12454--12456 completed successfully. The validated,
fixed Ti EOS reference is
`results/Ti_eos_benchmark/eos_reference/eos_reference.csv`; it remains
validation-only and must never enter a training DB.
