# Ti D0 Candidate Generation

Generate the approved Ti seed-perturbation POSCAR population, then label all
100 structures with the user-approved default static Protocol A through the
VASP batch workflow.
