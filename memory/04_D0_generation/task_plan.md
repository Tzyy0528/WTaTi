# Task Plan: D0 Generation: W, Ta, and Ti

This record groups the common workflow stage only. W, Ta, and Ti data, calculations, and outputs remain strictly independent.

## W

Generate the approved W seed-perturbation POSCAR population, then label all
100 structures with the user-approved default static Protocol A through the
VASP batch workflow.

## Ta

Generate the approved Ta seed-perturbation POSCAR population, then label all
100 structures with the user-approved default static Protocol A through the
VASP batch workflow.

## Ti

Generate the approved Ti seed-perturbation POSCAR population, then label all
100 structures with the user-approved default static Protocol A through the
VASP batch workflow.
